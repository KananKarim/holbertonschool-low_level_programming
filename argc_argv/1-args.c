#include <stdio.h>

/**
 * main - function
 * @argc: the number of the arguments
 * @argv: the array of arguments
 * Return: true
 */

int main(int argc, char *argv[])
{
	(void)argc;
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
